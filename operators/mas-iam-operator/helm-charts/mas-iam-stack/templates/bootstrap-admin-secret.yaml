{{- $bootstrap := .Values.keycloak.bootstrapAdmin -}}
{{- if $bootstrap.createSecret -}}
  {{- fail "keycloak.bootstrapAdmin.createSecret is no longer supported; create the secret yourself and set keycloak.bootstrapAdmin.secretName" -}}
{{- end }}
