{{- if and .Values.openldap.enabled .Values.openldap.admin.createSecret -}}
  {{- fail "openldap.admin.createSecret is no longer supported; create the secret yourself and set openldap.admin.secretName" -}}
{{- end }}
